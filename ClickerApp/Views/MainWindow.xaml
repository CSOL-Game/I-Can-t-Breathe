<Window
    x:Class="Views.MainWindow"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ViewModels"
    Title="ClickerApp"
    Icon="/Assets/avalonia-logo.ico"
    WindowState="Maximized"
    HasSystemDecorations="True"
    MaxWidth="270" MaxHeight="480">
    <Design.DataContext>
        <vm:MainWindowViewModel />
    </Design.DataContext>
  <Grid ColumnDefinitions="9*,*" RowDefinitions="*,16*,*" ShowGridLines="True">
    <DockPanel Grid.Column="1">
      <Button Click="ExitApp"/>      
    </DockPanel>
    <Grid Grid.Row="1" RowDefinitions="6*,4*" ShowGridLines="True">
      <Image/>
      <StackPanel Grid.Row="1" x:Name="BtnPanel">
        <Button x:Name="Play" Height=""/>
        <Button x:Name="Options"/>
      </StackPanel>
    </Grid>
  </Grid>
</Window>
